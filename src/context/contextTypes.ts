export type AuthContextTypes = {};

export type LoginTypes = {
  email: string;
  password: string;
};

export type adminTypes = {
  id: number;
  name: string;
  email: string;
  password: string;
  createdAt: string;
  updatedAt: string;
};
